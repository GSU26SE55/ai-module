[pytest]
testpaths = tests
addopts = -ra --strict-markers --tb=short
timeout = 180
markers =
    integration: exercises multiple runtime components
    network: requires explicit external network access
