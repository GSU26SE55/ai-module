[tool.ruff]
target-version = "py311"
line-length = 100
extend-exclude = [
  ".venv",
  "notebooks",
  "src/grpc_gen",
]

[tool.ruff.lint]
select = ["E4", "E7", "E9", "F", "I"]

[tool.ruff.lint.per-file-ignores]
"tests/**/*.py" = ["E402"]
"scripts/**/*.py" = ["E402"]
